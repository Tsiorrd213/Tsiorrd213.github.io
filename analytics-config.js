/*
 * AICA analytics configuration
 *
 * 1. Create a free project at https://clarity.microsoft.com/
 * 2. Copy the project ID from Settings > Setup.
 * 3. Replace the placeholder below and publish this file with the website.
 *
 * No visitor data is sent while the placeholder is unchanged.
 */
window.AICA_ANALYTICS_CONFIG = Object.freeze({
    clarityProjectId: 'xpucx5wtqt'
});
